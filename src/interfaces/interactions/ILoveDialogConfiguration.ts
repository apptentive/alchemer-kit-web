export interface ILoveDialogConfiguration {
  title: string;
  yes_text: string;
  no_text: string;
  show_dismiss_button: boolean;
  // TODO: map specific strings for the positions available
  position: string;
}
