/**
 * Renders a given Message Center footer.
 *
 * @returns {HTMLElement} - The footer DOM Node.
 */
const renderFooter = () => {
  const footer = document.createElement('footer');
  footer.classList.add('apptentive-footer');

  return footer;
};

export default renderFooter;
