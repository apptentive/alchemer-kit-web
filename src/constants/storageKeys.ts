export enum sessionStorageKeys {
  manifestCache = 'apptentiveCachedManifest',
  manifestExpiration = 'apptentiveCacheExpiration',
  manifestLocale = 'apptentiveLocaleOverride',
}

export enum localStorageKeys {
  appId = 'app_id',
  captureDisabled = 'captureDisabled',
  codePoint = 'code_point',
  conversation = 'conversation',
  device = 'device',
  interactionCounts = 'interaction_counts',
  person = 'person',
  random = 'random',
}
